-- ============================================================
--  kamanotes 演示数据 · 脚本 3b/4：分类笔记（不绑定题目）
--  分类：1Redis 2MySQL 3SQL 4MQ 5Java 6Spring 7JVM
--        8计算机网络 9操作系统 10数据结构与算法 11其他
-- ============================================================

USE kamanote_tech;
SET NAMES utf8mb4;

SET @u_zhangwei   = (SELECT user_id FROM user WHERE account='zhangwei');
SET @u_lina       = (SELECT user_id FROM user WHERE account='lina');
SET @u_wangfang   = (SELECT user_id FROM user WHERE account='wangfang');
SET @u_liuyang    = (SELECT user_id FROM user WHERE account='liuyang');
SET @u_chenjie    = (SELECT user_id FROM user WHERE account='chenjie');
SET @u_zhaomin    = (SELECT user_id FROM user WHERE account='zhaomin');
SET @u_sunpeng    = (SELECT user_id FROM user WHERE account='sunpeng');
SET @u_zhouxin    = (SELECT user_id FROM user WHERE account='zhouxin');
SET @u_wulei      = (SELECT user_id FROM user WHERE account='wulei');
SET @u_zhengshuang= (SELECT user_id FROM user WHERE account='zhengshuang');
SET @u_xulei      = (SELECT user_id FROM user WHERE account='xulei');
SET @u_hejuan     = (SELECT user_id FROM user WHERE account='hejuan');
SET @u_guoyu      = (SELECT user_id FROM user WHERE account='guoyu');
SET @u_maqiang    = (SELECT user_id FROM user WHERE account='maqiang');
SET @u_linna      = (SELECT user_id FROM user WHERE account='linna');

INSERT INTO note (author_id, question_id, category_id, content, created_at, updated_at) VALUES

-- ---------------- Redis ----------------
(@u_zhangwei, NULL, 1,
'# 缓存穿透、击穿、雪崩的区别与应对\n\n这三个概念面试超高频，但很多人说不清区别。核心在于**失效的原因不同**。\n\n## 缓存穿透\n**现象**：查询一个数据库里也不存在的 key，缓存永远不生效，请求全部打到 DB。\n\n**应对**：\n1. 缓存空值（设置较短过期时间，比如 60s）\n2. 布隆过滤器提前拦截\n3. 接口层做参数校验（比如 id <= 0 直接拒绝）\n\n## 缓存击穿\n**现象**：某个**热点 key 突然过期**，大量并发请求同时打到 DB。\n\n**应对**：\n1. 互斥锁：只让一个线程去加载，其他线程等待\n2. 逻辑过期：不设置 TTL，异步更新\n\n## 缓存雪崩\n**现象**：**大量 key 同时过期**，或 Redis 整个挂了。\n\n**应对**：\n1. 过期时间加随机值，打散失效时间\n2. 多级缓存（本地 Caffeine + Redis）\n3. Redis 高可用（哨兵 / 集群）\n4. 限流降级兜底\n\n> 一句话记忆：穿透是**查不存在的**，击穿是**一个热点过期**，雪崩是**一大批同时过期**。', '2026-07-05 10:20:00', '2026-07-06 09:10:00'),

(@u_lina, NULL, 1,
'# Redis 常用数据结构与典型场景\n\n| 结构 | 底层 | 典型场景 |\n| --- | --- | --- |\n| String | SDS | 缓存对象、计数器、分布式锁 |\n| Hash | 哈希表/ziplist | 存储对象字段、购物车 |\n| List | quicklist | 消息队列、最新列表 |\n| Set | 哈希表/intset | 去重、共同好友（交并集） |\n| ZSet | 跳表 + 哈希表 | 排行榜、延迟队列 |\n\n## 几个实践要点\n\n**分布式锁**\n```\nSET lock_key unique_value NX PX 10000\n```\n- `NX`：不存在才设置，保证互斥\n- `PX`：过期时间，防止死锁\n- `unique_value`：释放时校验，防止误删别人的锁（配合 Lua 保证原子性）\n\n**排行榜**\n```\nZADD rank 100 user:1\nZREVRANGE rank 0 9 WITHSCORES\n```\n\n**计数与限流**\n`INCR` 是原子的，适合做计数器；配合 `EXPIRE` 可以做固定窗口限流。', '2026-07-08 15:40:00', '2026-07-08 15:40:00'),

(@u_wangfang, NULL, 1,
'# Redis 持久化：RDB 与 AOF 怎么选\n\n## RDB（快照）\n- 把某一时刻的内存数据**二进制**写入磁盘\n- 优点：文件紧凑、恢复快、对性能影响小（fork 子进程）\n- 缺点：**可能丢数据**（两次快照之间的写入会丢）\n\n## AOF（追加日志）\n- 记录每一条**写命令**\n- 优点：数据安全性高，`appendfsync everysec` 最多丢 1 秒\n- 缺点：文件大、恢复慢\n\n## 刷盘策略\n| 配置 | 含义 | 安全性 |\n| --- | --- | --- |\n| always | 每条命令都 fsync | 最高，性能最差 |\n| everysec | 每秒 fsync | 折中，**生产常用** |\n| no | 交给操作系统 | 最快，可能丢较多 |\n\n## 生产建议\n**两者都开**。Redis 4.0 之后支持混合持久化（AOF 重写时以 RDB 格式作为基底），恢复时先加载 RDB 再重放增量 AOF，兼顾速度与安全。\n\n> 重启恢复优先级：如果同时开启，**优先用 AOF** 恢复（数据更完整）。', '2026-07-11 20:15:00', '2026-07-12 10:05:00'),

-- ---------------- MySQL ----------------
(@u_liuyang, NULL, 2,
'# MySQL 索引为什么用 B+ 树\n\n## 对比几种结构\n| 结构 | 范围查询 | 磁盘 IO | 说明 |\n| --- | --- | --- | --- |\n| 哈希表 | 不支持 | 1 次 | 等值查询 O(1)，但无法范围查询 |\n| 二叉搜索树 | 支持 | 高 | 可能退化成链表 |\n| 平衡二叉树 | 支持 | 高 | 树高 logn，每个节点一次 IO |\n| B 树 | 支持 | 较低 | 非叶子节点也存数据 |\n| **B+ 树** | **支持** | **最低** | 数据只在叶子，叶子间有链表 |\n\n## B+ 树的三个优势\n1. **树更矮**：非叶子节点只存索引不存数据，一个 16KB 页能放更多键，3~4 层就能存千万级数据\n2. **范围查询快**：叶子节点用双向链表串起来，范围扫描只需顺序遍历\n3. **查询稳定**：任何查询都要走到叶子，IO 次数一致\n\n## 聚簇索引与回表\n- **聚簇索引**（主键）：叶子节点直接存整行数据\n- **二级索引**：叶子节点存主键值\n\n用二级索引查非索引列时，要先查到主键再回聚簇索引取数据，这就是**回表**。\n避免回表的办法是**覆盖索引**（查询列都在索引里）。', '2026-07-14 09:30:00', '2026-07-15 16:20:00'),

(@u_chenjie, NULL, 2,
'# MySQL 事务隔离级别与 MVCC\n\n## 四种隔离级别\n| 级别 | 脏读 | 不可重复读 | 幻读 |\n| --- | --- | --- | --- |\n| 读未提交 RU | 会 | 会 | 会 |\n| 读已提交 RC | 不会 | 会 | 会 |\n| **可重复读 RR** | 不会 | 不会 | **InnoDB 基本不会** |\n| 串行化 | 不会 | 不会 | 不会 |\n\nMySQL 默认是 **RR（可重复读）**。\n\n## MVCC 的核心：ReadView + undo log\n每行记录有两个隐藏列：\n- `trx_id`：最后一次修改它的事务 ID\n- `roll_pointer`：指向 undo log 中的旧版本\n\n查询时生成一个 ReadView，包含当前活跃事务 ID 列表。判断规则：\n- `trx_id < 最小活跃 ID` → 已提交，可见\n- `trx_id >= 最大活跃 ID` → 之后才开始的事务，不可见\n- 在活跃列表中 → 不可见\n\n不可见就顺着 `roll_pointer` 找上一个版本，直到找到可见的。\n\n## RC 与 RR 的区别\n- **RC**：每次 SELECT 都生成新的 ReadView\n- **RR**：只在第一次 SELECT 生成，整个事务复用 → 这就是可重复读的来源\n\n> 补充：RR 下幻读靠 **间隙锁（Next-Key Lock）** 解决，但快照读仍可能看到「幻影行」，所以严格说是「基本不会」。', '2026-07-18 14:10:00', '2026-07-19 11:45:00'),

-- ---------------- SQL ----------------
(@u_zhaomin, NULL, 3,
'# 慢 SQL 定位与优化思路\n\n## 第一步：定位\n```sql\n-- 开启慢查询日志\nSET GLOBAL slow_query_log = ON;\nSET GLOBAL long_query_time = 1;\n\n-- 查看慢查询数量\nSHOW STATUS LIKE \'Slow_queries\';\n```\n\n## 第二步：分析执行计划\n在 SQL 前加 `EXPLAIN`，重点看这几列：\n\n| 列 | 关注点 |\n| --- | --- |\n| `type` | 至少要 range，`ALL` 是全表扫描要优化 |\n| `key` | 实际用到的索引，为 NULL 说明没走索引 |\n| `rows` | 预估扫描行数，越小越好 |\n| `Extra` | 出现 `Using filesort` / `Using temporary` 需警惕 |\n\n## 第三步：常见优化手段\n1. **加索引**：where / order by / join 的字段\n2. **避免索引失效**：\n   - 不要在索引列上用函数：`WHERE YEAR(create_time) = 2026`\n   - 不要以 `%` 开头做模糊匹配：`LIKE %abc`\n   - 注意隐式类型转换：字符串列传数字\n3. **覆盖索引**：把要查的列放进索引，避免回表\n4. **联合索引最左前缀**：`(a,b,c)` 能命中 `a`、`a,b`、`a,b,c`，不能跳过 a 直接用 b\n5. **分页优化**：`LIMIT 1000000, 10` 很慢，可以改成基于上次最大 id 的游标分页\n\n## 一个真实例子\n深分页 `LIMIT 1000000,10` 会扫描前 100 万行再丢掉。改成：\n```sql\nSELECT * FROM t WHERE id > 1000000 ORDER BY id LIMIT 10;\n```', '2026-07-22 19:05:00', '2026-07-23 08:40:00'),

-- ---------------- MQ ----------------
(@u_sunpeng, NULL, 4,
'# 消息队列选型对比与常见用途\n\n## 三大用途\n1. **异步**：注册后发邮件/短信，不阻塞主流程\n2. **解耦**：上游不用关心下游有多少消费者\n3. **削峰**：秒杀请求先入队，后端按能力消费\n\n## 选型对比\n| 特性 | RabbitMQ | Kafka | RocketMQ |\n| --- | --- | --- | --- |\n| 吞吐量 | 万级 | **十万级以上** | 十万级 |\n| 延迟 | 微秒级 | 毫秒级 | 毫秒级 |\n| 顺序消息 | 一般 | 分区内有序 | 支持 |\n| 事务消息 | 支持 | 较弱 | **支持** |\n| 适用场景 | 业务解耦 | 日志/大数据 | 电商交易 |\n\n## 消息可靠性三环节\n1. **生产者不丢**：confirm 机制 + 本地消息表\n2. **Broker 不丢**：持久化 + 副本（Kafka 的 acks=all，RocketMQ 的同步刷盘）\n3. **消费者不丢**：手动 ACK，处理成功再确认\n\n## 幂等性\n消息可能重复投递，消费端必须幂等：\n- 用唯一业务 ID 做去重表\n- 数据库唯一索引兜底\n- Redis `SETNX` 记录已处理的消息 ID\n\n> 面试常问「消息重复怎么办」——答案一定是**消费端幂等**，而不是指望 MQ 不重复。', '2026-07-25 11:30:00', '2026-07-26 15:10:00'),

-- ---------------- Java ----------------
(@u_zhouxin, NULL, 5,
'# ArrayList 与 LinkedList 怎么选\n\n| 操作 | ArrayList | LinkedList |\n| --- | --- | --- |\n| 随机访问 get(i) | **O(1)** | O(n) |\n| 头部插入 | O(n) | **O(1)** |\n| 尾部插入 | 均摊 O(1) | O(1) |\n| 内存占用 | 连续数组，更省 | 每个节点额外两个指针 |\n| CPU 缓存友好 | **友好** | 差（内存分散） |\n\n## 结论\n**绝大多数场景都应该用 ArrayList**，原因：\n1. 实际业务中随机访问远多于中间插入\n2. 即使中间插入，ArrayList 的 `System.arraycopy` 是内存批量拷贝，非常快\n3. LinkedList 节点内存分散，CPU 缓存命中率低，实测常常更慢\n\n## ArrayList 扩容\n- 默认容量 10（**第一次 add 时才分配**，不是 new 的时候）\n- 扩容为原来的 **1.5 倍**：`newCapacity = oldCapacity + (oldCapacity >> 1)`\n- 扩容是 `Arrays.copyOf`，所以**已知大小时应指定初始容量**避免多次扩容\n\n> 注意：`Arrays.asList()` 返回的是固定长度的内部类，不能 add/remove；要可变请用 `new ArrayList<>(Arrays.asList(...))`。', '2026-07-28 16:20:00', '2026-07-28 16:20:00'),

(@u_wulei, NULL, 5,
'# synchronized 与 ReentrantLock 对比\n\n## 相同点\n- 都是**可重入**锁\n- 都保证可见性与原子性\n\n## 不同点\n| 维度 | synchronized | ReentrantLock |\n| --- | --- | --- |\n| 实现层面 | JVM 关键字 | JDK 层 AQS |\n| 锁释放 | 自动（异常也释放） | **必须手动 unlock** |\n| 可中断 | 不支持 | `lockInterruptibly()` |\n| 超时获取 | 不支持 | `tryLock(timeout)` |\n| 公平锁 | 只有非公平 | 可选公平/非公平 |\n| 条件变量 | 单一 wait/notify | 多个 Condition |\n\n## 怎么选\n- 能用 **synchronized 就用它**：写法简单、不会忘记释放、JVM 持续优化（偏向锁、轻量级锁、锁消除）\n- 需要**超时、可中断、多条件队列**时才用 ReentrantLock\n\n## 使用 ReentrantLock 的正确姿势\n```java\nlock.lock();\ntry {\n    // 临界区\n} finally {\n    lock.unlock();   // 必须放 finally\n}\n```\n\n> 锁只能降级不能升级：持有读锁时可以去获取写锁，反之会死锁。', '2026-07-30 21:15:00', '2026-07-31 10:00:00'),

-- ---------------- Spring ----------------
(@u_zhengshuang, NULL, 6,
'# Spring IoC 与 AOP 一句话讲清\n\n## IoC（控制反转）\n对象的创建和依赖注入交给容器，而不是自己 `new`。\n\n**好处**：解耦、便于替换实现、统一管理生命周期。\n\n**三种注入方式**：\n1. 构造器注入（**官方推荐**）：依赖不可变、便于测试、能发现循环依赖\n2. setter 注入：适合可选依赖\n3. 字段注入 `@Autowired`：写法简单但不推荐（无法用 final、脱离容器难测试）\n\n## AOP（面向切面）\n把日志、事务、权限这类**横切关注点**从业务代码中抽离。\n\n**核心概念**：\n- 切面 Aspect、连接点 JoinPoint、切入点 Pointcut、通知 Advice\n\n**两种代理**：\n- **JDK 动态代理**：目标类实现了接口时使用，基于 `InvocationHandler`\n- **CGLIB**：目标类没实现接口时使用，生成子类字节码\n\nSpring Boot 2.x 默认 `proxyTargetClass=true`，也就是**强制使用 CGLIB**。\n\n## 一个常见的坑\nAOP 基于代理，所以**同类内部方法调用不会走代理**，导致 `@Transactional`、`@Async` 失效。解决办法是从容器里拿代理对象，或者拆到另一个 Bean。', '2026-08-01 14:50:00', '2026-08-02 09:25:00'),

(@u_xulei, NULL, 6,
'# @Transactional 失效的六种场景\n\n这是面试和实际开发都极易踩的坑。\n\n## 1. 同类内部调用\n```java\npublic void a() { b(); }        // 直接调，不走代理\n@Transactional\npublic void b() { ... }\n```\n**原因**：自调用不经过代理对象。\n\n## 2. 方法不是 public\nSpring 事务基于代理，`private` / `protected` / `static` 方法上的注解会被忽略。\n\n## 3. 异常被自己 catch 掉\n```java\n@Transactional\ntry { ... } catch (Exception e) { log.error(e); }  // 事务不会回滚\n```\n默认只在抛出 `RuntimeException` / `Error` 时回滚，checked 异常不回滚，且被吞掉就更不会回滚。\n\n## 4. 异常类型不对\n抛的是 checked 异常且没写 `rollbackFor`。正确写法：\n```java\n@Transactional(rollbackFor = Exception.class)\n```\n\n## 5. 传播行为设置不当\n`Propagation.NOT_SUPPORTED` 会以非事务方式执行。\n\n## 6. 多数据源未指定事务管理器\n存在多个 `PlatformTransactionManager` 时，不指定就会用错。\n\n> 自查口诀：**代（代理）公（public）吞（吞异常）类（异常类型）传（传播）源（数据源）**。', '2026-08-04 20:35:00', '2026-08-05 13:12:00'),

-- ---------------- JVM ----------------
(@u_hejuan, NULL, 7,
'# JVM 运行时内存区域\n\n## 线程私有\n| 区域 | 作用 | 异常 |\n| --- | --- | --- |\n| 程序计数器 | 当前执行字节码的行号 | 唯一不会 OOM |\n| 虚拟机栈 | 存放栈帧（局部变量表、操作数栈） | StackOverflowError / OOM |\n| 本地方法栈 | 为 native 方法服务 | 同上 |\n\n## 线程共享\n| 区域 | 作用 | 异常 |\n| --- | --- | --- |\n| **堆** | 对象实例，GC 主战场 | OutOfMemoryError |\n| 方法区（元空间） | 类信息、常量、静态变量 | OOM |\n\n## 堆的分代\n```\n堆 = 新生代(Eden + S0 + S1) + 老年代\n默认比例：新生代 1/3，老年代 2/3\nEden : S0 : S1 = 8 : 1 : 1\n```\n\n## 对象什么时候进老年代\n1. 年龄达到阈值（默认 15，每次 Minor GC 存活 +1）\n2. 大对象直接进老年代\n3. 动态年龄判断：Survivor 中相同年龄对象总和超过一半\n\n## 常用排查命令\n- `jps`：查看 Java 进程\n- `jstat -gc <pid> 1000`：实时看 GC 情况\n- `jmap -dump:format=b,file=heap.hprof <pid>`：导出堆快照\n- `jstack <pid>`：查看线程栈，排查死锁\n\n> 注意：JDK 8 之后方法区由**元空间**实现，使用本地内存，不再受 `-XX:MaxPermSize` 限制。', '2026-08-07 10:40:00', '2026-08-08 15:55:00'),

(@u_guoyu, NULL, 7,
'# 垃圾回收算法与常见收集器\n\n## 判断对象是否可回收\n**可达性分析**：从 GC Roots 出发，不可达的对象才可回收。\n\nGC Roots 包括：\n- 虚拟机栈中引用的对象\n- 方法区静态属性引用的对象\n- 方法区常量引用的对象\n- 本地方法栈 JNI 引用的对象\n\n（引用计数法无法解决循环引用，所以 Java 不用它。）\n\n## 三种基础算法\n| 算法 | 优点 | 缺点 |\n| --- | --- | --- |\n| 标记-清除 | 简单 | 产生内存碎片 |\n| 复制 | 无碎片、高效 | 浪费一半空间 |\n| 标记-整理 | 无碎片 | 移动对象成本高 |\n\n## 分代收集\n- **新生代**：对象朝生夕死 → **复制算法**（Eden + 两个 Survivor）\n- **老年代**：存活率高 → **标记-清除 / 标记-整理**\n\n## 常见收集器\n| 收集器 | 区域 | 特点 |\n| --- | --- | --- |\n| Serial | 新生代 | 单线程，适合客户端 |\n| ParNew | 新生代 | Serial 的多线程版 |\n| Parallel Scavenge | 新生代 | 关注吞吐量 |\n| CMS | 老年代 | 关注停顿，并发标记清除，有碎片 |\n| G1 | 全堆 | **JDK 9+ 默认**，可预测停顿 |\n| ZGC | 全堆 | 停顿 < 10ms，超大堆 |\n\n> G1 把堆划分成大小相等的 Region，通过 `-XX:MaxGCPauseMillis` 设定目标停顿时间，优先回收收益最高的 Region（Garbage First）。', '2026-08-10 17:20:00', '2026-08-11 09:50:00'),

-- ---------------- 计算机网络 ----------------
(@u_maqiang, NULL, 8,
'# TCP 三次握手与四次挥手\n\n## 三次握手\n```\nClient                Server\n  |---- SYN seq=x ------->|      (我要连你)\n  |<-- SYN+ACK seq=y ack=x+1 --|  (收到，我也要连你)\n  |---- ACK ack=y+1 ----->|      (收到，开始传数据)\n```\n\n**为什么是三次而不是两次？**\n两次的话，服务端无法确认客户端收到了自己的 SYN。如果客户端的历史 SYN 延迟到达，服务端会建立一个无效连接并一直等待，浪费资源。第三次 ACK 让服务端确认「客户端确实在等我」。\n\n## 四次挥手\n```\nClient                Server\n  |---- FIN ------------->|   (我没数据要发了)\n  |<--- ACK --------------|   (知道了)\n  |<--- FIN --------------|   (我也发完了)\n  |---- ACK ------------->|   (知道了，关闭)\n```\n\n**为什么是四次？**\nTCP 是全双工的，一方不再发送数据不代表另一方也发完了。收到 FIN 后服务端可能还有数据要发，所以 ACK 和 FIN 不能合并。\n\n## TIME_WAIT 为什么是 2MSL\n1. 保证最后一个 ACK 能到达对端（丢了还能重发）\n2. 让本次连接的旧报文在网络中自然消失，不干扰新连接\n\n## 常见面试追问\n- **SYN 洪泛攻击**：伪造大量 SYN 不给 ACK，占满半连接队列 → 用 SYN Cookie 防御\n- **为什么不能是四次握手**：三次已经足够确认双方收发能力，多一次是浪费', '2026-08-13 20:10:00', '2026-08-14 11:30:00'),

(@u_linna, NULL, 8,
'# HTTP 与 HTTPS 的区别\n\n## 核心区别\nHTTPS = HTTP + TLS/SSL，在传输层之上加了**加密、完整性校验、身份认证**。\n\n| 维度 | HTTP | HTTPS |\n| --- | --- | --- |\n| 端口 | 80 | 443 |\n| 加密 | 明文 | 对称 + 非对称混合 |\n| 证书 | 不需要 | 需要 CA 证书 |\n| 性能 | 略快 | 多一次 TLS 握手 |\n\n## TLS 握手（简化）\n1. 客户端发送支持的加密套件 + 随机数\n2. 服务端返回**证书** + 随机数\n3. 客户端验证证书（CA 链、域名、有效期），取出公钥\n4. 客户端生成 pre-master secret，用**公钥加密**后发给服务端\n5. 双方用三个随机数生成**对称密钥**\n6. 之后用对称密钥通信\n\n## 为什么要混合加密\n- **非对称加密**（RSA/ECDHE）安全但慢 → 只用来**交换密钥**\n- **对称加密**（AES）快 → 用来**加密数据**\n\n这样既安全又高效。\n\n## 常见追问\n- **证书的作用**：解决「公钥是不是真的属于这个网站」的问题，靠 CA 的签名背书\n- **中间人攻击**：没有证书校验时，攻击者可以替换公钥；有 CA 校验则无法伪造签名\n- **HTTPS 一定安全吗**：不，只能保证传输安全；服务端被拖库照样泄露', '2026-08-16 15:45:00', '2026-08-17 10:20:00'),

-- ---------------- 操作系统 ----------------
(@u_zhangwei, NULL, 9,
'# 进程与线程的区别\n\n## 本质区别\n- **进程**是资源分配的基本单位（有独立地址空间、文件描述符表）\n- **线程**是 CPU 调度的基本单位（共享进程资源，只有独立的栈和寄存器）\n\n## 对比表\n| 维度 | 进程 | 线程 |\n| --- | --- | --- |\n| 地址空间 | 独立 | 共享 |\n| 切换开销 | 大（切页表、刷 TLB） | 小 |\n| 通信方式 | IPC（管道、共享内存、消息队列） | 直接读写共享变量 |\n| 崩溃影响 | 互不影响 | 一个线程崩溃整个进程挂 |\n| 创建开销 | 大 | 小 |\n\n## 线程切换为什么比进程快\n进程切换要换页表 → 导致 **TLB 失效** → 缓存命中率骤降。线程共享地址空间，不需要切换页表。\n\n## 协程\n用户态的轻量级线程，由程序自己调度，切换不陷入内核，开销更小。适合 IO 密集型场景（如 Go 的 goroutine）。\n\n> 面试延伸：为什么 Redis 单线程还这么快？因为它纯内存操作 + IO 多路复用，避免了线程切换和锁竞争的开销。', '2026-08-19 09:05:00', '2026-08-19 09:05:00'),

(@u_lina, NULL, 9,
'# 死锁的四个必要条件与避免\n\n## 四个条件（缺一不可）\n1. **互斥**：资源同一时刻只能被一个进程占用\n2. **占有并等待**：持有资源的同时等待其他资源\n3. **不可剥夺**：资源不能被强行抢走\n4. **循环等待**：存在一个进程-资源的环形等待链\n\n## 破坏任一条件即可避免死锁\n| 条件 | 破坏方式 |\n| --- | --- |\n| 互斥 | 尽量用可共享资源（如只读文件） |\n| 占有并等待 | **一次性申请所有资源** |\n| 不可剥夺 | 申请不到就释放已持有的资源 |\n| 循环等待 | **按固定顺序申请资源**（最实用） |\n\n## 工程中最常用的做法\n**统一加锁顺序**。比如约定所有线程都按「先锁 A 再锁 B」的顺序，就不会形成环。\n\n## 排查手段\n```bash\njstack <pid> | grep -A 20 \'Found one Java-level deadlock\'\n```\nJVM 会自动检测并打印死锁的线程与持有的锁。\n\n> 注意：死锁和「活锁」「饥饿」不是一回事。活锁是线程都在动但没法推进；饥饿是优先级低的线程一直拿不到资源。', '2026-08-21 16:30:00', '2026-08-22 14:15:00'),

-- ---------------- 数据结构与算法 ----------------
(@u_chenjie, NULL, 10,
'# 常见排序算法对比与选择\n\n| 算法 | 平均 | 最坏 | 空间 | 稳定 | 特点 |\n| --- | --- | --- | --- | --- | --- |\n| 冒泡 | O(n²) | O(n²) | O(1) | 稳定 | 教学用 |\n| 选择 | O(n²) | O(n²) | O(1) | **不稳定** | 交换次数少 |\n| 插入 | O(n²) | O(n²) | O(1) | 稳定 | 小规模/近乎有序时很快 |\n| 希尔 | O(n^1.3) | O(n²) | O(1) | 不稳定 | 插入排序的改进 |\n| 归并 | **O(n log n)** | O(n log n) | O(n) | 稳定 | 外排序、链表排序 |\n| 快排 | **O(n log n)** | **O(n²)** | O(log n) | 不稳定 | 实际最快 |\n| 堆排 | **O(n log n)** | O(n log n) | O(1) | 不稳定 | TopK、找中位数 |\n| 计数 | O(n+k) | O(n+k) | O(k) | 稳定 | 值域小的整数 |\n\n## 快排最坏 O(n²) 怎么来的\n每次选的基准都是当前区间的最大或最小值，分区极度不平衡 → 退化成冒泡。\n\n**优化**：\n1. **随机化基准** 或三数取中\n2. 小区间改用插入排序（比如长度 < 10）\n3. 三路划分处理大量重复元素\n\n## 怎么选\n- 通用排序 → **快排**（工程实现常做混合优化）\n- 要求稳定 → **归并**\n- 只要 TopK → **堆**（O(n log k)）\n- 值域小且是整数 → **计数/桶排序**\n\n## Java 里的实现\n- `Arrays.sort(int[])` → 双轴快排（DualPivotQuicksort）\n- `Arrays.sort(Object[])` → TimSort（归并的优化版，稳定）\n\n> 这也解释了一个常见问题：为什么 `Arrays.sort` 对基本类型和对象类型用的算法不一样？因为对象排序要求**稳定**，基本类型不需要。', '2026-08-24 11:50:00', '2026-08-25 16:40:00'),

(@u_sunpeng, NULL, 10,
'# 时间复杂度速查与常见陷阱\n\n## 常见量级（由快到慢）\n```\nO(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ) < O(n!)\n```\n\n**经验换算**（1 秒约 10^8 次运算）：\n| n 规模 | 可接受的复杂度 |\n| --- | --- |\n| 10 | O(n!) |\n| 20 | O(2ⁿ) |\n| 100 | O(n³) |\n| 1000 | O(n²) |\n| 10^5 | O(n log n) |\n| 10^7 | O(n) |\n\n> 这个表在做题时很有用：看到 n = 10^5，就该想到 O(n log n)，基本是排序或二分。\n\n## 三个常见陷阱\n\n### 1. 只看到循环层数\n```java\nfor (int i = 1; i < n; i *= 2)   // 这不是 O(n)，是 O(log n)\n```\n\n### 2. 忘记 Java 字符串拼接的代价\n在循环里用 `s += x` 是 O(n²)，因为每次都创建新字符串。应该用 `StringBuilder`。\n\n### 3. 忽略递归的深度\n递归求斐波那契是 O(2ⁿ)，加备忘录后降到 O(n)。**递归树的分支数和深度都要看**。\n\n## 均摊分析\nArrayList 的 `add` 单次可能是 O(n)（触发扩容），但均摊下来是 O(1)。分析时不要只看最坏的单次操作。', '2026-08-26 19:10:00', '2026-08-26 19:10:00'),

-- ---------------- 其他 ----------------
(@u_zhaomin, NULL, 11,
'# 我的三个月秋招复习计划（附执行情况）\n\n## 背景\n双非本，目标后端开发，7 月开始系统准备，记录一下节奏供参考。\n\n## 第一阶段：打基础（7 月）\n- 每天 3 道算法题，按分类刷：数组 → 链表 → 哈希 → 字符串\n- 每道题都写笔记，**重点记录思路和易错点**，而不是抄题解\n- 周末复习当周笔记\n\n## 第二阶段：补八股（8 月）\n按模块过：Java 基础 → 集合 → 并发 → JVM → MySQL → Redis → 网络 → 操作系统\n- 每个模块先看笔记，再自己复述一遍\n- 用这个笔记系统按分类整理，复习时直接按标签筛\n\n## 第三阶段：项目 + 模拟（9 月）\n- 把课设项目吃透，能讲清技术选型和难点\n- 找同学互相模拟面试，重点练表达\n\n## 一点心得\n1. **笔记要自己写**，抄来的记不住\n2. **算法不要只刷不复习**，二刷比刷新题更有用\n3. **八股要理解原理**，死记硬背面试一追问就露馅\n4. 用分类管理笔记真的很省时间，复习时可以按「Redis」「JVM」直接筛\n\n> 目前进度：算法 280 题，二刷 120 题，八股过完 2 轮。继续加油。', '2026-09-05 21:30:00', '2026-09-06 10:15:00');

SELECT CONCAT('笔记总数 = ', COUNT(*), '（其中题目笔记 ',
       SUM(CASE WHEN question_id IS NOT NULL THEN 1 ELSE 0 END),
       ' 条，分类笔记 ',
       SUM(CASE WHEN category_id IS NOT NULL THEN 1 ELSE 0 END), ' 条）') AS result
FROM note;
